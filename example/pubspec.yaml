name: mapboxnav_example
description: "Demonstrates how to use the mapboxnav plugin."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.1
  mapboxnav:
    path: ../
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
